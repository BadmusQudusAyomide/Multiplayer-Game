NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
NEXT_PUBLIC_SOCKET_URL=http://localhost:4000

# Optional: controls opponent rejoin countdown UI (ms)
NEXT_PUBLIC_MATCH_REJOIN_GRACE_MS=15000

# Optional: force Socket.IO to use polling only (useful on hosts blocking WebSockets)
# Accepts: "true" | "1" to enable. Default is disabled (WebSocket preferred with polling fallback).
NEXT_PUBLIC_FORCE_POLLING=

# Note: when the site is served over HTTPS, the client will auto-upgrade
# http:// values above to https:// at runtime to avoid mixed content errors.
